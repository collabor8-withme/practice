// No.1 named export 命名导出
// export function printVersion(version) {
//     console.log(`项目的版本是${version}`)
// }

// No.2 default export 默认导出
function printVersion(version) {
    console.log(`项目的版本是${version}`)
}

export default printVersion
